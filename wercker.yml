box: ruby:2.3.1
services:
  -postgres
build:
  steps:
    - bundle-install
    - script:
        name: Prepare database
        code: bundle exec db:test:prepare
    - script:
        name: rspec
        code: bundle exec rspec
